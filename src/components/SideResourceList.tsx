import { styled } from 'styled-components'

const Container = styled('div')`
  overflow: scroll;
  width: 280px;
`

const SideResourceList = () => {
  return <Container>리소스 리스트</Container>
}

export default SideResourceList
